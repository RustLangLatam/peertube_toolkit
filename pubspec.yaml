name: peertube_toolkit
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  river_player: ^0.1.4
  infinite_scroll_pagination: ^4.1.0
  cached_network_image:
  timeago:
  shimmer:
  url_launcher:

  peer_tube_api_sdk:

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
